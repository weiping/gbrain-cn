# Bootstrap

Post-scaffold steps. gbrain displays this but does NOT auto-execute.
The agent reads it and walks per-step at its own discretion.

1. show user: "reference-pack is installed. Try one of the trigger phrases from skills/reference-pack/SKILL.md."
2. (edit me) agent: gbrain put_page wiki/_reference-pack-config --frontmatter type=config
